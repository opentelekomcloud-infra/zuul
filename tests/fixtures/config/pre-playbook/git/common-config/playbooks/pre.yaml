- hosts: all
  tasks:
    - copy:
        src: "{{zuul.executor.work_root}}/flag"
        dest: "{{zuul.executor.work_root}}/failed"
    - file:
        path: "{{zuul.executor.work_root}}/pre_flag"
        state: touch
