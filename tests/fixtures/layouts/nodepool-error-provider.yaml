# An error in the provider configuration

- flavor:
    name: normal

- image:
    name: debian
    type: cloud

- label:
    name: debian-normal
    image: debian
    flavor: normal

- section:
    name: aws-base
    connection: aws
    flavors:
      - name: normal
        instance-type: t3.large
    # Add a label that references an image unattached to this
    # provider.
    labels:
      - debian-normal

- provider:
    name: aws-us-east-1-main
    region: us-east-1
    section: aws-base
