- hosts: all
  roles:
    - dstat-graph
  tasks:
    - name: Collect zookeeper logs
      shell: "cp /opt/zookeeper/logs/* {{ zuul_output_dir }}/logs/"
    - name: Collect mariadb logs
      shell: "sudo /usr/bin/journalctl -u mariadb > {{ zuul_output_dir }}/logs/mariadb.log"
